# Required
OPENAI_API_KEY=XXX

# Optional
OPENAI_MODEL=XXX
PLAYWRIGHT_BROWSER=chromium
 
# Airtable MCP (required to enable Airtable server)
# Create a Personal Access Token with scopes:
#  - data.records:read, data.records:write
#  - schema.bases:read, schema.bases:write
# Limit access to specific workspaces/bases as needed.
AIRTABLE_API_KEY=
AIRTABLE_BASE_ID=XXX

# Advanced (optional)
# Override package or execution mode if needed
AIRTABLE_MCP_PACKAGE=@felores/airtable-mcp-server
AIRTABLE_USE_NPX=1